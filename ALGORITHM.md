# 共享边界的成组折线合并（v0.5）

## 目标与度量

不给城市设硬性点数配额。在形状误差预算和拓扑约束内，直接减少实际转角；线段始终使用 0/45/90/135°方向。各环去掉闭合重复点后计算非共线转角，共边在两个城市中分别计数，岛屿计入所属市。路径坐标数另行记录，不把删除共线坐标当成形状简化。

旧算法的几何固定点不等于最少折线：每条任意斜边重新规整时可能生成三段。新版从真实边界的首次规整结果（iteration_0）出发，直接替换整段链，不使用旧第9轮作为输入。

## 算法

1. 把城市边界合并、节点化，并在交会处分成共享链。按坐标确定稳定顺序。每条链只处理一次；固定链端点。闭合独立环本版保留，含九江两个极小离散区域。
2. 冻结初始链作为误差参考。每个候选都对照同一初始链，不能用上一步结果重置误差预算。
3. 枚举同一链上相隔至少三段的端点。尝试一段允许方向的直线，或两条允许方向直线的交点组成的两段线。交点不能超出端点包围盒。替换整个中间折线串，自动合并同向共线段。
4. 检查候选简单性、与其他链的接触位置不变、替换扫过的区域未吞入其他边界；新旧链互相落在18 SVG单位缓冲范围内。缓冲圆弧采用保守的内接多边形近似。
5. 候选排序：减少链点数最多、短于6 SVG单位的线段最少、偏移最小、坐标字典序。依次尝试所有局部可行候选；较长候选被全局约束拒绝时，仍继续尝试较短替换。
6. 重建全部城市面，检查有效覆盖、无重叠、城市及省域连通分量和孔洞、市际共边/点接触/共边连通段数、交会点的边界环绕顺序。所有交会点固定，不能交换城市围绕交点的顺序。
7. 检查各市相对真实参考的面积误差不超过15%，几何交并比IoU至少75%。另以真实边界进行双向缓冲覆盖检查：每市限度为首次规整与真实边界的测得偏移加18；具体限度写入报告。
8. 只有全图实际转角数严格减少时才接受替换。按固定顺序扫过共享链；完成一个全无修改的扫描即停止。

每次接受都严格降低一个非负整数，因此有限步结束，无需人为决定迭代轮数。允许的端点在合并过程中会变化，所以每次扫描重新产生候选。

## 保证范围

这是确定性的受约束贪心算法，停止表示当前几何上没有通过全部检查的上述单次替换。它不保证全局最优，也不等价于前期讨论的动态规划。先增加转角再合并、移动交会点、三段以上替代路径、闭合环整体重绘等操作不在本版搜索范围内。

形状不是完全不变；面积/IoU/距离预算约束变化。也不承诺所有狭窄部位宽度保持不变。各城市点数由形状与邻接复杂度自然产生，不要求统一达到10或20。

18单位是当前760×860画布中、地图高度约608单位下的原型参数，不能直接当成全国地图的经纬度或米数。迁移到全国应先统一投影和目标显示尺度，再使用同一全局共享线网，避免逐省独立处理造成省界裂缝。空间索引和局部重建可改善性能；当前全量重建优先便于验证。

## 可复现与验收

`python generate_map.py` 从保留的参考数据生成历史对照及默认长线合并版。`python simplify_boundaries.py --tolerance 18` 可单独重建长线版。`python validate_geometry.py` 检查实际交付SVG，而非只检查内存几何。

`dist/shortcut-report.json` 保存每条原始/最终共享链、每次接受后的转角总数、城市点数/形状指标、拒绝原因和停止原因。验证共享链并集等于交付边界，交点顺序和接触不变，累计误差预算满足，每步转角严格减少。


## v0.6追加阶段：小分量过滤与内角优化

新增 `soften_angles.py`，由统一生成入口在长线版之后调用。过滤每市非最大连通分量，须同时满足所属市面积占比<0.5%、全省占比<0.01%；不删除城市主体，不把被删除地块重新归给其他城市。参考与抽象分别过滤后检查签名一致；如果过滤导致两者拓扑不匹配则停止报错。本例仅九江两个微小离散分量符合阈值。

角度按多边形内部测量，将外环规范为逆时针、孔洞为顺时针，计算有符号转向。凸45°计锐角，凹315°不计；不能用两向量夹角的较小值代替多边形内角。

冻结过滤后的长线版共享链，保持端点和交会的环绕顺序。增加两类候选：沿尖角两侧截短后以允许方向连接；在交会点附近插入长度2/4/6/8的允许方向短首段，再接回原链。第二类可以修复单纯端点间单调路径无法处理的交会锐角。所有候选仍检查无交叉、接触相同、扫过区域不含其他边界，并双向处于冻结链8单位缓冲内。

目标采用词典序 `(锐角数, 转角数)`：先消除锐角，同锐角数时减少转角。全网选取目标最优的可行单次修改，依次以偏移、链序、坐标打破平局。每次严格改善非负整数目标，有限停止。候选仍须满足过滤后拓扑、各市面积误差≤15%、IoU≥75%。这不是任意地图都保证零锐角的证明；若约束冲突会保留残余锐角并记录。本次江西实际残余为0。

去尖角可能临时增加转角或短线，因此新阶段不再要求每一步单独降低转角数；它仍优先保持整体简洁。本次20次接受修改后15个锐角全部消除，最终主体转角数165，与过滤后的输入相同。未承诺所有飞地或孔洞都忽略，也未执行面积均衡。


## v0.7追加阶段：小凸起整合与三岔点联动

输入固定为去尖角版。本轮只将赣州、吉安、上饶作为短边优化目标；周围城市仍参与全部形状和拓扑检查。目标城市配置在 `compact_lobes.py`，不是针对凸起手写坐标。

小凸起既可能是离散地块，也可能仍有细窄连接。本例是后者，因此继续删除MultiPolygon小分量无效。新阶段允许三岔节点移动：从交点附近的已有角点及其水平/竖直支撑线交点生成位置，单次移动不超过24单位；同时枚举三条相接链的45°路径，必要时跳过最多两个近端旧点，整组提交。另允许整段链长线替换。

保留共享链身份和交会的循环顺序，不要求交会点坐标不动。重建所有市后检查过滤后的拓扑签名、有效覆盖、零锐角、各市面积误差≤15%、IoU≥75%。共享链和城市边界始终与冻结输入作24单位双向缓冲比较，避免逐步漂移。允许联动邻市，不允许新增空洞或重叠。

目标为 `(三市短于12单位的实际边数, 全图转角数)`，按词典序严格下降。同分时选择相对冻结输入对称差面积更小的候选，再按几何坐标打破平局。非负整数目标严格下降，有限终止；当前候选集合无改善时停止。短边是窄小凸起的启发式代理，不是通用窄颈检测或全局最优保证。

本例12次修改：目标(14,165)→(1,141)。赣州和上饶无短于12单位的边；吉安保留一个短台阶。独立验证交付SVG以及共享链溯源、移动后交会顺序、冻结误差预算、形状预算、零锐角和全部市际接触关系。

## v0.8追加阶段：全部市际共边的短线简化

`simplify_shared.py` 从主体整合版出发，统计全部19对市际共边。对每对城市先提取交集、合并连续共线段，再统计实际线段数；每条共边只计一次。多处不连续接触分别保留，不能为减少段数抹掉其中一处。短边筛查阈值为12 SVG单位，城市总转角不设配额。

新目标按词典序为 `(全部共边短线数, 全部共边线段数, 全图城市转角数)`。使用已有共同求解器：整段链替换只针对内部共享链，交会点移动允许联动外边界。除一两段候选外，加入三段正交折线及居中斜线候选（沿跨度的1/4、1/2、3/4处），保留合适端点走向，避免更短的二段路径产生锐角。交会点仍须整组三链联动，不能逐市独立修改。

所有候选保持零锐角、45°方向、过滤后的市际共边/点接触/共边连通段数、各市和省域的连通分量/孔洞及交会环绕顺序。与固定主体整合版共享链及城市边界做24单位双向缓冲检查；每市对真实参考面积误差≤15%、IoU≥75%。没有用前一步结果重置位移预算。复用固定链的缓冲检查缓存，减少重复计算。

每次接受严格改善三项整数目标，扫描至候选无改善即停止；局部边界可能增加一段以消除短折返，最终记录每对共边前后统计，不能仅用全图总数掩盖局部变化。剩余短边表示当前候选及约束下未找到可行改进，不宣称几何上永远不能简化。完整记录在 `dist/shared-report.json`。


## v0.9：固定既有省份，扩展相邻省份

1. 沿用既有球面墨卡托投影和缩放，每市保留面积最大的连通主体；完整参考仍显示全部岛屿。形状预算与拓扑签名相对筛选后主体，完整来源IoU另行记录。
2. 以6单位覆盖简化和0.5单位网格做初次45°贴合；孤立城市若初始IoU小于80%或面积误差超过10%，按6、4、2、1、0.5逐步降低简化容差。共享链合并沿用18单位预算与每市IoU75%、面积误差15%约束。
3. 从真实边界中寻找距离既有省份边界1单位内的最长连续弧，将端点投影到冻结省界。新省复用其间的冻结弧，替换自身西侧弧；公共线网重新节点化，允许增加共线点但禁止修改旧省几何。
4. 去尖角时冻结旧省及接壤链；闭合岛屿轮廓拆成两个弧参与优化。采用固定次序的首个可行改善，必要时加入0.5、1单位步长。每次检查45°、覆盖、接触及交会顺序、形状预算，并验证旧省几何完全相等。
5. 全部公共短线使用v0.8目标，允许新省三岔点联动，累计位移限制18单位。每阶段整数目标严格下降，候选无改善时停止；这是局部终止，不保证全局最少线段。

`validate_east.py` 检查实际序列化数据：22市、无重叠、主体拓扑、上饶—衢州唯一跨省接触及精确共享路径、冻结江西、45°、零锐角、标签、画布范围、形状预算与共边统计。原始省界微小不一致通过识别接壤弧处理，不把原始数据直接拼贴视为无缝。


## v0.10：岛群概括与可读性放大

舟山全部源多边形按面积加权质心做确定性三类k-means。初始中心为最大岛，后续选择面积乘到最近中心距离平方最大者；分配不变时停止。每组用45°切角矩形概括，目标包围矩形面积为源组面积1.6倍且至少450单位，长宽比由加权质心方差确定并限制在1～1.8。切角后实际面积另行记录。

从原聚类中心开始，仅当岛块间距不足6单位时，以最大组中心为锚逐步放大中心间距，每次0.05，最大1.6倍；检查与大陆间隔同样至少6。输出三块保持独立，其他市完全不参与变形。舟山作为用户授权的示意放大，豁免此前主岛IoU/面积预算，但报告真实误差；其余城市预算和所有市际接触仍严格检查。


## v0.11：同时接合两个已冻结省份

福建沿用全局投影与尺度。识别新省各市真实边界距离既有两省真实外框1单位内的连续弧；选择最长接壤弧，源端点对应到已简化福建的省界交会点，再投影到冻结外框。所有共享端点统一映射，内部共享边使用相同45°连接路由，外部接壤弧直接复用冻结外框。重建一次共享线网，并校验全部既有城市几何相等及真实接触签名。

跨省关系由源边界0.05单位邻近带交集长度超过3单位识别，福建本例10对；交付时独立验证全部关系及每条共享弧完全一致。此源数据容差用于消除数据源的微小省界不一致，不允许新增邻接。

冻结外框可能让新省继承45°夹角：本例两个点位于已冻结的双市交会处。只允许初始实际存在且落在冻结外框上的这两个锐角坐标；候选不得在其他位置产生锐角。保留冻结几何和完整共享边优先于消除这两个角，报告明确列出例外。

福建每市先取最大主体。另保留面积至少100单位且占本市至少5%的源离岛，本例厦门一块。离岛采用8角切角矩形，包围面积为源面积1.25倍且至少160单位；按距源质心最小的整数偏移搜索，最多24单位，确保距所有大陆/岛块至少2.5单位。位移及实际面积单列，主体预算不适用于示意离岛。共享边简化仍相对冻结阶段输入控制18单位偏移，不设城市点数配额。


## v0.12：湖南与自治州、微小孔洞处理

湖南沿既有三省西侧接入，冻结31个区划。13个地级市和湘西土家族苗族自治州均作为市级单元；图面显示“湘西”，交互保留全称和自治州类型。

主体准备阶段延续最大连通块规则，另填平同时小于20 SVG平方单位、占所属主体小于0.1%的内部孔洞。本例省略岳阳、怀化各一个微小离散块，填平怀化约10.32平方单位的孔洞。完整真实参考保留全部环；路径读取按SVG偶奇填充处理嵌套孔洞，避免将孔洞误读为岛屿。

对于省界源数据的短暂断续，仅在断续段沿边界总长不超过6单位、每个点距离既有真实省界不超过2单位时桥接，其他位置仍用1单位接壤弧判定。此规则恢复长沙的一小段省界，仍须通过全部真实邻接和冻结几何检查，不允许引入新接触。

共享线网重建采用空间索引筛选包围框相交的候选区划，之后仍按最大面积交叠归属，同面积时取最小原始序号。与原全量遍历在45个区划上的结果逐项比较一致，减少地图扩展后的重复计算。

继承尖角检查区分旧图已存在的锐角与新省在冻结省界的锐角；独立验证新省例外必须对应冻结外框315°内角留下的45°空间，不以任意残余锐角作为豁免依据。

湖南最终合并按固定候选顺序接受首个可行改善，再重新扫描，直到完整扫描无改善。目标仍为短共边数、共边总段数、全图转角数的严格词典序下降，不设轮数或城市点数配额。冻结区划的内部边界也直接跳过，避免生成注定被拒绝的候选。可选检查点以输入几何、预算、目标和选择策略的哈希校验恢复，保留最初的误差参照，不能借恢复过程重置累计位移预算。

湖南内部两市的交会位置可沿同一个冻结市的边界重新分配接壤段，但不得移动旧市轮廓、改变相邻关系，或改变整条省际共享线。本例长沙—株洲交会位置沿萍乡边界调整9单位；报告同时保留初始和最终各市接壤弧，验证全部接壤弧的并集完全一致。


## v0.13：安徽与飞地填补

固定江西、浙江、福建、湖南45个区划，安徽16市向北接入，继续使用同一投影与共享线网。主体准备阶段先记录所有被省略的小分量；内部孔洞若99.9%以上由这些已省略分量覆盖，则随之填入周围主体，避免省略飞地后留下人工空洞。其余孔洞沿用面积小于20单位且占主体小于0.1%的规则。安徽本例省略淮北一块、铜陵两块；铜陵两块分别嵌在安庆、池州内，另填平池州两个微小孔洞。完整真实参考保留全部原始分量与孔洞。

省界拼接会消耗形状误差预算，本轮在拼接前采用82%的主体IoU下限预留余量，拼接后仍执行75%与15%面积误差的统一交付门槛。共享简化器新增可选的minimum_iou参数，默认75%，既有省份的调用不变。所有转角由形状及拓扑约束决定，不设市级点数配额。

去尖角阶段也直接跳过全部冻结城市的边界，包括旧省内部共边；冻结几何相等检查仍保留。该剪枝只排除必然违反冻结约束的候选。

安徽最终转角200个，范围7～17；共享阶段31次改善，目标(63,336,776)→(44,315,736)，无可行候选时停止。宣城仅保留冻结杭州—湖州交会外框315°内角对应的一处45°空间；独立验证允许点来自冻结几何，不能将任意残余尖角列为例外。


## v0.14：广东与沿海较大离岛

广东21市接入湖南、江西、福建南侧，冻结既有61个区划。保留源数据最大主体，延续微小孔洞与省略飞地孔洞的填补规则。初次贴合依次尝试6、4、2、1、0.5单位覆盖简化容差，选取第一个同时满足源主体拓扑与82%形状重合度的结果；本例使用4单位，避免小型沿海城市在第一阶段就耗尽预算。后续仍遵循75%主体IoU、15%面积误差及18单位累计偏移预算。

离岛概括每市最多保留一个块：选择最大非主体分量，源面积达到100 SVG平方单位时保留。这样保留广东本例汕头、江门、湛江、汕尾四个较大离岛，同时省略其余细碎岛块。采用已有八边形拟合与最小平移搜索，和所有主体及岛块至少相隔2.5单位。此岛块属于示意概括，单独记录面积及位移，不混入主体形状预算。完整真实参考保留全部分量与孔洞。

广东初次共享链简化启用可选的固定次序首个可行改善策略，先扫描较短的局部窗口，再逐步增加窗口跨度，延迟生成候选，避免先穷举和排序整个复杂海岸线的所有方案。每次仍严格减少总转角并通过原有拓扑、形状、交会顺序与累计偏移检查；完整扫描无改善时停止。默认策略保持不变，既有省份生成器不受此开关影响。

市际接触签名采用容量8192的有界缓存，以不可变几何对为键。几何变化会产生新键，旧城市和未改动城市复用精确交集结果；82区划的完整接触签名已与缓存前实现逐项比较一致。

初次简化也逐轮原子保存检查点。恢复时核对初始几何、真实主体、误差预算和策略的哈希，重建已保存线网并检查拓扑及交会顺序；累计偏移始终相对最初输入。完整扫描无改善的状态也保留，已完成的阶段不会重新计算。

初次简化阶段还分别缓存不可变区划几何与源主体的IoU、边界偏移判断（各2048项）。误差参考与阈值固定在阶段初始值，缓存仅复用相同几何的判断，不改变接受条件或候选次序。

拼接后若某市超出形状预算，自动找出该市关联的已简化共享链，并按恢复链数从少到多枚举恢复组合。对应原始链以相同端点和最小Hausdorff距离识别，并检查一一对应及交会环绕顺序；恢复的是初次45°贴合的原始链，省际冻结边仍精确复用。每个组合重新建立共享线网、拼接省界，并验证全部真实邻接、冻结几何和各市形状预算；接受首个全部通过的组合，完整记录恢复的源链。广东潮州在漳州冻结边界附近触发了此步骤，不手写调整坐标。

对冻结缓冲区建立预备几何索引，并缓存局部候选的几何检查。其他边界只在该缓冲区的包围框内参与局部交叉与扫过区域检查（扫过区域不可能超出候选与原链的包围框）；完整区划拓扑和形状预算仍全局检查。60条链上161个候选的局部筛选结果已与全网检查比较一致。广东命令行生成过程另使用互斥锁，防止重复启动同一省份计算。

去尖角和最终共边阶段同样预先建立固定边界缓冲区，避免在每个候选上重复构建；最终共边阶段缓存相同区划几何的形状与偏移判断。交付检查同时核对全图并集无内部空洞，并确认全图连通分量仅增加明确保留的离岛。

广东本例拼接恢复两条源链；最终主体254个转角（7～18个/市），四个离岛另计32个。共边阶段32次改善，目标(86,461,1041)→(62,433,990)，随后加入离岛得到全图1022个转角。全部191对邻接保持，无重叠与并集内部空洞；主体最低IoU75.07%，最大面积误差13.68%。韶关、梅州各继承一处冻结省界留下的45°夹角，全图共六处已验证的例外，未增加其他锐角。

## v0.15 实验：固定方向共享线网的面积均衡

以v0.14抽象地图作为独立基准。先将全部边界共同节点化并重建82区划的各个面，得到615个共享顶点、695条边。每边法向位移差为零，通过约束矩阵零空间参数化，仅优化535个自由度，方向保持不变。边长下限为原长25%与2 SVG单位中的较小值，避免边翻转或消失。

近邻非关联线段（初始距离50 SVG单位以内）以原始最近点方向建立分离半平面。固定边方向和正边长使投影极值端点不变，故每对只需一条最严格不等式。相同连通块间隔下限为原间隔20%与2单位中的较小值；不同连通块至少保留2.5单位。共3953条线性不等式，邻近筛选仅用于求解约束，最终全图独立检查不省略远处对象。

目标面积为 C×A^α，C使目标总面积等于基准总面积。每个独立面按所属区划目标比例分配，防止仅扩大离岛满足整个城市指标。损失包含各面相对目标面积的平方误差、每条有向边相对所属城市等比例缩放向量的偏差，以及弱顶点位移正则；边形状项权重0.3，位移项0.005/顶点数，坐标按100单位归一化。面积不作为强等式，最终总面积允许有小量误差。

α按0.95、0.90、0.85、0.80、0.75继续求解，使用SLSQP、解析梯度与固定线性约束，单档上限400步、ftol=1e-9；每次候选独立检查有效覆盖、邻接、连通分量、内部孔洞、岛间距和锐角，不通过则对上一档结果回溯。本次五档均完整接受，均以优化器成功状态结束；不声称全局最优或任意目标可行。末次220步。

交付路径保留8位小数，独立验证器检查完整拓扑签名、逐市转角数及锐角数、45°方向、基准轮廓和标签不变、无重叠、无新增孔洞、离岛海面间隔及画布。浮点路径的共线判定采用角度相对容差，避免将小数舍入误差算作新转角。

结果面积比25.033→11.276，目标11.191；总面积+0.0694%。归一化形状IoU中位数92.92%，单块区划最低上饶77.29%，多块区划最低舟山63.40%。整体岛群IoU同时反映岛间布局变化，不能直接视为岛块本身变形量；本次留给人工对照检阅，不额外手工修形。

## v0.16：极小接壤单元与直辖市

从未均衡的v0.14基础图添加香港、澳门、上海。对每个最大主体按6、4、2、1、0.5、0.25、0.1容差尝试源轮廓简化，再以0.125网格贴合45°；选取IoU至少75%、面积误差不超过15%的初值。闭环按固定次序尝试替换连续段为一或两段45°路径，优先降低锐角数、其次转角数，接受首个可行改善，直至无可行改善，不设市级点数配额。

新单元与旧海岸重叠较小时，划分共享线网后从相邻旧区划扣除重叠，本例仅深圳约15.09平方单位。没有接触时，枚举方向平行的新旧边界支持线，搜索20单位内最小刚性平移，要求不重叠、形成至少3单位共边、源IoU至少75%；上海平移2.375单位接合嘉兴。

极小单元的深嵌入会被面积均衡成倍放大，因此澳门改用靠海侧接壤示意：同样最小平移搜索，但不限制原位置IoU，并将最短共边设为1单位，得到向东9.75单位的平移。此为明确记录的位置例外，不借平移重置真实形状指标；澳门拟合形状和面积保持，真实参考不移动。珠海基础轮廓完整保留。

上海最大非主体块独立按上述简化规则拟合，其余两个较大源岛屿合并为一个八角示意块。两个显示岛块按最小整数平移搜索、距全部陆地及岛屿至少2.5单位、位移不超过24单位；其余小岛省略。共边节点化后用浮点数往返精度输出，避免四位小数舍入切割交点造成缝隙。

均衡器去掉固定82区划限制，接收独立源文件和输出前缀。软面积下限取初步目标中位数的25%，所有目标最终统一归一化；直接求解α＝0.75，最多800步并记录停止状态。保持共享图、各边方向和正长度，防碰撞与离岛间隔规则延续。完整拓扑签名、逐市转角与锐角、标签、画布和参考完整性由交付文件独立验证。历史82区划默认生成参数保持不变。

本例直接求解595步成功，面积比716.352→10.726，总面积+0.0698%。85区划共1087转角，均衡后逐市转角数不变、194对邻接及完整拓扑签名不变。珠海归一化形状IoU87.46%，澳门99.86%；澳门面积约放大51.28倍，单独记录其可读性示意。香港82.83%、上海87.12%，全图形状指标中位数92.41%。
